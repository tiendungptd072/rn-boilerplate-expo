# Local Expo config and runtime environment. EXPO_PUBLIC values are not secrets.
APP_VARIANT=development
EXPO_PUBLIC_APP_ENV=development
EXPO_PUBLIC_API_URL=http://localhost:3000

# Required by production app.config and supplied by each project's EAS environment.
EXPO_PROJECT_ID=
